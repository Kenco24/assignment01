import swap
import extras.desc

a=int(input("Enter first number to swap"))
b=int(input("Enter second number to swap"))
print(swap.swapping(a,b))

c=int(input("Enter number to print in descending order"))
extras.desc.desending(c)
